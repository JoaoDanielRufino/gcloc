// main function
const main = () => {
  console.log("Hello, World!");

  // This is a single block comment for testing purpose.

  const a = 2;
  const b = 3;
  // Sum a + b
  // That will be sum up a total of a + b, another comment
  const total = a + b;

  /* Multi line
  comment
  for testing purpose.
  */

  console.log(total);
};

main();
// main function
const main = () => {
  console.log("Hello, World!");

  // A comment block.

  const a: number = 2;
  const b: number = 3;
  // Sum a + b
  const total: number = a + b;

  // New single comment block for test

  console.log(total);

  /* This is multiple comment
  line of block
  for testing.
   */
};

main();
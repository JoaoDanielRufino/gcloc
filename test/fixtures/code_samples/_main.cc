#include <iostream>

using namespace std;

// main function
int main(int argc, char *argv[]) {
    std::cout << "Hello World!" << std::endl;

    auto a = 2;
    auto b = 3;
    // Sum a + b
    auto total = a + b;

    std::cout << total << std::endl;

    return 0;
}

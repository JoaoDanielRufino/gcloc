#include <iostream>

using namespace std;

// main function
int main(int argc, char *argv[]) {
    std::cout << "Hello World!" << std::endl;

    // One line of comment block for testing

    auto a = 2;
    auto b = 3;
    // Sum a + b
    auto total = a + b;

    /* This is multiple line
    of comment block
    for testing*/

    std::cout << total << std::endl;

    // end of line comment block

    return 0;
}
#include <iostream>

using namespace std;

// main function
int main(int argc, char *argv[]) {
    std::cout << "Hello World!" << std::endl;

    // Single line of block comment for testing

    auto a = 2;
    auto b = 3;
    // Sum a + b
    auto total = a + b;

    /* Another line of comment block
    with multiple line
    for testing purpose */

    std::cout << total << std::endl;

    return 0;
}